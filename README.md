Use 'make clean' to clean the bin folder

then either
    use 'make' to compile the application
    use 'make run' to run the application

or
    use 'make mrun' to make and then run the application using one command